mixin statusDay(note, styleMarker, isRevert)
   - note = note || 'Status';

   if isRevert
      .note=note
      .marker-day(style=styleMarker)
   else
      .marker-day(style=styleMarker)
      .note=note
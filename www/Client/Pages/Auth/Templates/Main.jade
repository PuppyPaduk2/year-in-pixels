include ../../../Page/Template.jade

block headBefore
   +routepage()

block bodyAfterContent
   //- Кнопки навигации
   .buttons-nav
      .button(type="link", name="sing-in")="Sing in"
      .button(type="link", name="sing-up")="Sing up"

   //- Формы редактирования
   .forms
      .name-project="Year in pixels"
      .form(name="sing-up")
         input(type="text", name="login", placeholder="Login")
         input(type="password", name="password-1", placeholder="Password")
         input(type="password", name="password-2", placeholder="Password")
         .button(type="block")="Send"